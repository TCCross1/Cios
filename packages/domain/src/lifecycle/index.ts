/**
 * @cios/domain lifecycle
 *
 * Ordinary entity lifecycle state — deliberately separate from (and not a
 * substitute for) any future Canon authority system. See
 * `lifecycle-state.ts` for the full distinction.
 */
export { type LifecycleState, isLifecycleState, resolveLifecycleState } from './lifecycle-state.js';
