/**
 * @cios/domain rule
 *
 * Foundational `Rule` — identity/name/description only. A Rule's
 * existence never implies Canon authority (see `rule.ts`).
 */
export { type Rule, type CreateRuleInput, createRule } from './rule.js';
