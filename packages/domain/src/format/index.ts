/**
 * @cios/domain format
 *
 * `CreativeFormat` — the closed set of creative media a `CreativeWork` can
 * be produced for.
 */
export { type CreativeFormat, isCreativeFormat, assertCreativeFormat } from './creative-format.js';
