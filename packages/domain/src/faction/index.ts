/**
 * @cios/domain faction
 *
 * Foundational `Faction` — identity/name/description only. See
 * `faction.ts` for the Faction/Organization modeling decision (Directive
 * 003 implements only `Faction`).
 */
export { type Faction, type CreateFactionInput, createFaction } from './faction.js';
