/**
 * @cios/domain errors
 *
 * The domain's single error vocabulary for deterministic construction/
 * validation failures (Constitution, section F). This module has one
 * bounded purpose — it is not a general-purpose error-handling framework.
 */
export { DomainValidationError } from './domain-validation-error.js';
