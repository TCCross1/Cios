/**
 * @cios/domain concept
 *
 * Foundational `Concept` — identity/name/description only. Must not
 * become a generic fallback for unmodeled objects (see `concept.ts`).
 */
export { type Concept, type CreateConceptInput, createConcept } from './concept.js';
