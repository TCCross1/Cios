{
  "name": "@cios/worker",
  "version": "0.1.0",
  "private": true,
  "description": "CIOS background/long-running job runtime. Architecture foundation only, Directive 002 — no queue implementation.",
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@cios/application": "workspace:*",
    "@cios/infrastructure": "workspace:*",
    "@cios/agent-runtime": "workspace:*",
    "@cios/contracts": "workspace:*",
    "@cios/config": "workspace:*"
  }
}
