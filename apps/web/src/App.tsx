/**
 * @cios/web root component.
 *
 * Placeholder-only. This is not the CIOS product interface — no design
 * system, routing, or product UI exists yet (Directive 002 is
 * architecture/topology only). It exists solely to prove the React + Vite
 * toolchain works end to end.
 */
import type { ReactElement } from 'react';

export function App(): ReactElement {
  return (
    <main>
      <h1>CIOS</h1>
      <p>Web workspace foundation is running.</p>
    </main>
  );
}
